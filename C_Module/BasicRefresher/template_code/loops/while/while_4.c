#include <stdio.h>

int main()
{
    while (1)  //1 - non boolean, converts to true in boolean, it is always true, hence infinite loop
    {
        printf("hello\n");
    }
    return 0;
}

// infinite loop