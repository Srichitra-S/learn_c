#include <stdio.h>

int main()
{
    int i=25;
    while (i = i/2)  
    {
        printf("%d\n",i);
    }
    return 0;
}

// the expression only calculates
// there is nothing to compare here
// the expression or values are converted to boolean/non-zero meaning it is always true